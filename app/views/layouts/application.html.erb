<!DOCTYPE html>
<html>
<head>
  <title>A rails blog</title>
  <link href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<header class="jumbotron site-header">
  <div class="container text-center">
    <h1>My Rails Blog</h1>
  </div>
</header>

<div class="container">
  <%= yield %>
</div>

<footer class="footer-bar">
  <div class="container text-center">
    <% if user_signed_in? %>
    Logged in as: <%= current_user.email %> <%= link_to('Logout', destroy_user_session_path, method: :delete) %>
    <% else %>
    Not logged in. <%= link_to('Click here to log in', new_user_session_path) %>
    <% end %>
  </div>
</footer>
</body>
</html>
